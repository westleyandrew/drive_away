<div class="container">
  <div class="row" style="width:100%">
    <% @bookings.each do |booking| %>
      <div class="col-sm-3">
        <h2><%= link_to booking.car.title, booking_path(booking) %></h2>
        <%= booking.user.email %>
        <%= booking.booking_start_date %>
        <%= booking.booking_end_date %>
      </div>
    <% end %>
  </div>
</div>
<p><%= link_to "Back to car list", cars_path %></p>
